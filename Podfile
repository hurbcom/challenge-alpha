# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

target 'HurbAlpha' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HurbAlpha
  pod 'Moya', '~> 13.0'
  pod 'Kingfisher'
  pod 'SnapKit', '~> 5.0.0'

  target 'HurbAlphaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HurbAlphaUITests' do
    # Pods for testing
  end

end
