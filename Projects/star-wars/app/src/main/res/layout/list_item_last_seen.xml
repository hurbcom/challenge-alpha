<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="100dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/screen_padding_extra_small"
    android:layout_marginEnd="@dimen/screen_padding_extra_small">

    <ProgressBar
        android:id="@+id/item_last_seem_progress"
        android:indeterminate="true"
        android:layout_gravity="center"
        style="?android:attr/progressBarStyleSmall"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <ImageView
        android:id="@+id/item_last_seem_image"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:contentDescription="@string/grid_img_last_seen_description"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"

        tools:src="@drawable/logo_yellow" />


    <TextView
        android:id="@+id/item_last_seem_title"
        android:layout_width="@dimen/zero_dp"
        android:layout_height="50dp"
        android:layout_marginBottom="@dimen/screen_padding_medium"
        android:ellipsize="end"
        android:gravity="center_horizontal|center_vertical"
        android:maxLines="2"
        android:paddingStart="@dimen/screen_padding_medium"
        android:paddingEnd="@dimen/screen_padding_medium"
        android:textColor="@color/black"
        android:textSize="@dimen/title_size_medium"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/item_last_seem_image"
        tools:text="@tools:sample/last_names" />

</androidx.constraintlayout.widget.ConstraintLayout>